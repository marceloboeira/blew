	<form action="/account/update/<%=account.id%>" method="POST" class="edit">
	<div class="form-group">
		<h2> editing a account... </h2>
		<% if(flash && flash.err) { %>
<ul class="alert alert-success">
<% Object.keys(flash.err).forEach(function(error) { %>
<li><%- JSON.stringify(flash.err[error]) %></li>
<% }) %>
</ul>
<% } %>
		
	<div class="form-group" style="width: 400px;">
	<label>name</label>
	<input value="<%= account.name %>" name="name" type="text" class="form-control"/>
	</div>

	<div class="form-group" style="width: 400px;">
	<label>name</label>
	<input value="<%= account.age %>" name="age" type="text" class="form-control"/>
	</div>

	<div class="form-group" style="width: 400px;">
	<label>name</label>
	<input value="<%= account.email %>" name="email" type="text" class="form-control"/>
	</div>

		</div>
		<input type="submit" value="Update Account" class="btn btn-default"/>
		<a href="/account/show/<%=account.id %>" class="btn btn-danger">Cancel</a>
	</form>
	
	