<div class="container">
	<div class="row">
		<div class="col-xs-3 col-sm-3 col-md-3 col-lg-3">
		</div>

		<div class="col-xs-6 col-sm-6 col-md-6 col-lg-6" >
			<h1 class="page-header">Not a member?</h1>
			<p class="lead">Your should join us! Choose below how your want to do it.</p> 
				<a pjax href="/auth/github" class="btn btn-default btn-lg  " >
					<i class="fa fa-github-square"></i> <%= __("Github") %>
				</a>
		</div>
	</div>
	<hr/>
	<div class="container">
	<h3><%= __('Last Pastes') %> </h3>
	<table class="table table-condensed table-striped"> 
		<thead>	
			<tr>
				<th><%= __('Name') %> </td>
				<th><%= __('Language') %> </td>
				<th><%= __('Owner') %> </td>
				<th><%= __('Created') %> </td>
				<th><%= __('Updated') %> </td>
			</tr>
		</thead>
		<tbody>
		<% lastPastes.forEach(function(paste) { %>
			<tr>
				<td><a pjax href="<%= paste.getLink() %>"> <%= paste.name %> </a></td>
				<td><%= paste.language %></td>
				<td><a pjax href="<%= paste.owner.getLink() %>"><%= paste.owner.name %></a></td>
				<td><date source="<%= paste.createdAt %>"><%=: paste.createdAt | dateFromNow %></date></td>
				<td><date source="<%= paste.updatedAt %>"><%=: paste.updatedAt | dateFromNow %></date></td>
			</tr>
		<% }); %>
		<tbody>
	</table>
	</div>				
	<hr>
</div>